"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createCache = createCache;

var _fs = _interopRequireDefault(require("fs"));

var _path = _interopRequireDefault(require("path"));

var _mkdirp = _interopRequireDefault(require("mkdirp"));

var _util = _interopRequireDefault(require("util"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const writeFile = _util.default.promisify(_fs.default.writeFile);

const mkdirp = _util.default.promisify(_mkdirp.default);

function createCache({
  config
}) {
  return {
    async writeCacheFile(filePath, content) {
      await mkdirp(config.cachePath);
      await writeFile(_path.default.join(config.cachePath, filePath), content);
    },

    async createDirectory(dirPath) {
      await mkdirp(_path.default.join(config.cachePath, dirPath));
    }

  };
}